# 배달 손익 자동화 프로그램 — 인수인계 문서

## 1. 무엇을 하는 프로그램인가
가맹점(79대포 등)의 **배달 손익계산서**를 자동으로 만들어 주는 프로그램입니다.
여러 사이트에 흩어진 데이터를 자동으로 모아 → 매장별/채널별(배민·쿠팡)/월별 손익을
정해진 엑셀 양식에 채워 **바탕화면에 저장**합니다.

- 실행: 바탕화면 **「배달 손익 생성기」** 아이콘 더블클릭 → 매장 선택 → "손익 보고서 생성"
- 결과: `바탕화면\{매장명}\{YYMM 매장명}.xlsx`  (예: `바탕화면\방이점\2605 방이점.xlsx`)
- 저장소(코드): https://github.com/thebutm2-art/-

## 2. 데이터 소스 4종 → 손익 보고서
| 소스 | 가져오는 것 | 수집 방식 |
|---|---|---|
| 배민셀프서비스 | 정산명세서(수수료·배달비·광고비·부가세·매출) | 자동: 정산명세서를 메일로 발송 |
| Gmail(thebut_m2@79daepo.com) | 위 정산명세서 첨부 수신 | 자동: 브라우저로 수신·다운로드 |
| 토더(ToOrder) | 메뉴별 판매량·매출(배민/쿠팡 채널) | 자동: Playwright |
| 쿠팡이츠 포털 | 쿠팡 정산내역서 | 자동: Playwright(봇우회) |
| 식부자재 단가표(월별 엑셀) | 메뉴별 식자재/부자재 단가 | 수동: 월 1개 파일 폴더에 둠(공통) |

흐름: **배민셀프 발송 → Gmail 수신 → 암호해제 → 파싱 → 토더·쿠팡 수집 → 식부자재 원가매칭 → 보고서 저장**

## 3. 손익 계산 기준
### 배민 (정산명세서 기반)
- 매출 = 바로결제주문금액 + 가게배달팁 + 보정금액
- 식자재/부자재 = 토더 메뉴판매량 × 식부자재 단가
- 광고비(중개) = 배민1+알뜰+가게배달+픽업 중개이용료
- 광고비(우가클) = 우리가게클릭 + 부가세
- 카드/어플수수료 = 결제정산수수료 기본수수료(정률)
- 부가세 = 부가세
- 배달비 = 배민1한집 + 알뜰배달비 + (가게배달 건수 × 4,400)
- 프로모션 = 주문금액 즉시할인
- 공과금 = 매출 × 5% (고정)
- 순수익 = 매출 − (식자재~공과금)

### 쿠팡 (쿠팡이츠 정산내역서 열 합산)
- 매출=K열 · 광고비(중개)=Q열 · 배달비=V열 · 카드/어플수수료=S열 · 부가세=AG열 · 광고비(맞춤)=AK열
- 식자재/부자재 = 토더(쿠팡 채널) × 식부자재

## 4. 사용법
1. 바탕화면 「배달 손익 생성기」 더블클릭
2. ① 매장 선택 (한 개만 고르면 ②에서 매장정보 편집 가능)
3. ② 배민파트너명·파일암호 입력 (처음 1회, 생성 누르면 마스터에 저장됨)
   - 파일암호 = 정산서 엑셀 잠금해제용 (일반/간이=생년월일6자리, 법인=법인번호 뒷7자리)
4. ③ 대상 월(예: 2026-05)
5. ④ Gmail은 브라우저 자동 수신(앱 비밀번호 불필요, 첫 1회 창에서 로그인)
6. "손익 보고서 생성" 클릭 → 바탕화면에 저장

## 5. 설치 (새 PC) — `새PC_설치가이드.md` 참고
```
1) git clone https://github.com/thebutm2-art/-.git
2) pip install -r requirements.txt  /  python -m playwright install chromium  (+ Chrome 설치)
3) .env.example → .env 복사 후 입력(TOORDER_ID/PW, MAIL_USER)
4) 점포마스터_v2.xlsx 준비(계정정보로 생성 또는 기존 PC에서 복사)
5) 식부자재_{월}월.xlsx 폴더에 두기
6) python make_shortcut.py  → 바탕화면 아이콘 생성
7) 첫 실행 시 Gmail/배민/쿠팡 로그인 1회
```

## 6. 보안 주의
- `.env`(비밀번호), 점포마스터(계정·암호), 정산서·매출 엑셀, `.browser_profile`(세션)은
  **민감정보** → 깃에 올리지 않음(`.gitignore`로 제외). 외부 공유 금지.

## 7. 현재 상태 (2026-06 기준)
**검증 완료**
- 방이점·강릉교동점: 배민셀프 발송 → Gmail 수신 → 해제 → 파싱 → 토더 → 보고서 (end-to-end)
- 토더 자동수집(배민/쿠팡 채널), 식부자재 원가매칭(메뉴매칭 ~98%)
- 쿠팡 봇차단 우회 로그인 성공

**남은 보완**
- 쿠팡 정산내역서 자동다운: 매장선택 단계 셀렉터 보정 필요(쿠팡 수수료 섹션)
- 가게배달 건수: 현재 정산서 카운트(폴백) 사용 → 가게통계 정확값 연동 예정
- 배민셀프/쿠팡은 매장마다 계정이 달라, 2단계 인증/캡차 시 해당 창에서 1회 직접 로그인 필요

## 8. 주요 파일
- `손익생성기.pyw` : GUI(메인 프로그램)   /   `run_report.py` : 처리 오케스트레이터
- `step_baemin_self_send.py` 배민셀프 발송 · `step_gmail_browser.py` Gmail 수신
- `step4_parse_settlement.py` 배민 파싱 · `step_coupang_parse.py` 쿠팡 파싱
- `step_toorder.py` 토더 · `step_coupang_fetch.py` 쿠팡 다운 · `cost_matcher.py` 원가매칭
- `step7_final_report.py` 양식 채움 · `store_master.py`/`import_accounts.py` 마스터
- `점포마스터_v2.xlsx` 매장 설정 · `식부자재_{월}월.xlsx` 단가표
